import styled from 'styled-components';

import { Colors } from './constants';
import { Block } from './helpers';

export const Separator = styled(Block)({
  height: 1,
  backgroundColor: Colors.black,
});
