import Config, { NativeConfig } from 'react-native-config';

export const CONFIG: NativeConfig = {
  API_URL: Config.API_URL,
  BOOT_SPLASH_DURATION: Config.BOOT_SPLASH_DURATION,
  RESEND_TIMEOUT: Config.RESEND_TIMEOUT,
};
