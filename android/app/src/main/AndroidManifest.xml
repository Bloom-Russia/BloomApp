<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.QUIT_ALL_PACKAGES"/>
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES"/>

    <application
            android:name=".MainApplication"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:allowBackup="false"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="${usesCleartextTraffic}"
            android:supportsRtl="true">

        <!-- Активность заставки -->
        <activity
                android:name="com.zoontek.rnbootsplash.RNBootSplashActivity"
                android:exported="true"
                android:launchMode="singleTask"
                android:theme="@style/BootTheme">
        </activity>

        <!-- Главная активность -->
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name"
                android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
                android:launchMode="singleTask"
                android:networkSecurityConfig="@xml/network_security_config"
                android:screenOrientation="portrait"
                android:windowSoftInputMode="adjustPan"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Для работы сервиса notifee -->
        <service
                android:name="app.notifee.core.ForegroundService"
                android:foregroundServiceType="location|camera|microphone|phoneCall"
                android:stopWithTask="false"/>

        <service android:name="app.notifee.core.NotifeeApiService"/>

        <!-- Для работы с событиями в фоне -->
        <receiver
                android:name="app.notifee.core.BootReceiver"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <receiver
                android:name="app.notifee.core.Receiver"
                android:exported="false">
            <intent-filter>
                <action android:name="app.notifee.ACTION_PRESS"/>
                <action android:name="app.notifee.ACTION_DISMISS"/>
            </intent-filter>
        </receiver>

        <!-- Метаданные для иконки уведомлений -->
        <meta-data
                android:name="com.google.firebase.messaging.default_notification_icon"
                android:resource="@drawable/notification_icon"/>

        <!-- Цвет акцента для уведомлений (опционально) -->
        <meta-data
                android:name="app.notifee.default_notification_color"
                android:resource="@color/notification_color"/>

        <!-- Для совместимости с Android 13+ -->
        <meta-data
                android:name="com.dieam.reactnativepushnotification.notification_color"
                android:resource="@color/notification_color"/>

        <!-- Для совместимости с notifee -->
        <meta-data
                android:name="app.notifee.default_notification_color"
                android:resource="@color/notification_color"/>

        <uses-library
                android:name="org.apache.http.legacy"
                android:required="false"/>

    </application>
</manifest>
